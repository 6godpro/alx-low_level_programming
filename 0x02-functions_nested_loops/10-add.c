#include "main.h"

/**
 * add - function that adds two integers and returns the result.
 * @a: integer variable
 * @b: integer variable
 * Return: returns the sum of a and b
 */

int add(int a, int b)
{
	int add = a + b;

	return (add);

}

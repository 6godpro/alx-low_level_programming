#include "main.h"

/**
 * mul - multiplies the values of two integers
 * @a: integer value 1
 * @b: integer value 2
 * Return: the value of the result
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}

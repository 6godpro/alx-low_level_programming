#include <stdio.h>

/**
 * main - Entry point
 * Description - This program prints the string using puts()
 * function
 * Return: Always 0 (success)
 */

int main(void)
{
	char *str = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}

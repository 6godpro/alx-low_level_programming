#include <stdio.h>

/**
 * main - Entry point
 * Description - prints string to stdout using printf()
 * Return: Always 0 (success)
 */

int main(void)
{
	char *str = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", str);
	return (0);
}
